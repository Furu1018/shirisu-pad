# 最適凸プラン v3 設計計画 — キャラ被りを考慮したユニオン全体打点最大化

> **ステータス**: 計画中（実装なし）。フロー = **Fable試行（本文書 = 第1案）→ Opus5 再検討 → Codex 併用で最終計画**。
> 各フェーズはこの文書に追記していく。着手はこの文書が固まってから。

## 背景・目的（なぜやるか）

現行の最適凸プランは「総与ダメ最大化（Lv4無限ボス・温存・吸収）」まで進化済みだが、**キャラ被り（同キャラは1日1回）をプレイヤー単位で最適に扱えていない**。

- **今日起きた実例（ラピ問題）**: 「ラピ」は灼熱(fire)PT・鉄甲(iron)PT両方で使える優秀キャラ。灼熱にも鉄甲にもラピ入り編成を提出 → 先に**鉄甲凸（ラピ使用）**を実行 → 最適プランを押したら**ラピ入りの灼熱PTが提案され、ラピは使用済みで凸できなかった**。
- **狙い**: メンバーに**とにかく多くの編成（5属性×各2編成）を出してもらい**、ソルバーが**キャラ被りを避けつつユニオン全体の合計打点を最大化する組み合わせ**をワンボタンで割り出す。

### ドメイン洞察（ユーザー提供・重要）
- NIKKE編成では **B1/B2（サポート）は環境で固定＝優秀な数体をみんなが使う** → **被りは主にB1/B2で起きる**。B3（アタッカー）は属性ごとに決まる。
- そのため**プレイヤー自身「どの3属性の組み合わせが最高打点になるか」が分からない**ことが多い。→ 全パターン出せばソルバーが**最適な3凸を自動で割り出す**、が理想。

### 本命の運用フロー：「部分消化 → 残りお任せ」
- 朝に**自信のある1属性で1凸だけ済ませる**（優秀なB1/B2を消費）→ その後「**残り2凸は運営指示にお任せ**」というメンバーが多い。
- 運営は**ワンボタンで、朝に使ったキャラを除外した組み合わせで残り2凸の最適プラン**を出したい。
- → **この運用フローで唯一足りていないのがフェーズ1（完了凸キャラの除外）**。現行ソルバーは「残り凸数の割り当て」は既にできているため、**フェーズ1単体でこのフローのほぼ全価値が出る**。

---

## 現状のギャップ（実コードに基づく）

### ① 【正しさ】完了済み凸のキャラが `usedChars` に未反映（ラピバグの根本原因）
- `js/optimal-plan.js`: 各メンバーの `usedChars` は**空で初期化**され、**プラン算出中に自分が割り当てた凸のキャラしか入らない**（:246 宣言 / :415, :559 追加）。
- **実際に完了した凸で消費したキャラ**（朝の鉄甲凸で使ったラピ）は `usedChars` に入っていない。
- `usedCount`（:151, :193-198）は「その属性を何回凸したか」で**同属性の上位編成を消費済み扱い**にするだけ。**属性をまたいだキャラ消費**（鉄甲のラピが灼熱でも使えない）は見ていない。
- → 完了済みの鉄甲ラピが灼熱プランに反映されず、ラピ入り灼熱を提案してしまう。
- ※ プラン算出中の自分の中では被り回避は効く（usedChars はパス内で属性横断共有）。**穴は「押す前の実凸のキャラ消費が未反映」の1点**。

### ② 【最適化】ボス順の貪欲で、プレイヤー単位の「どの3属性に3凸を割くか」を大域最適化していない
- 現行は各ボスに対し「被りなしで最高打点の編成」を貪欲に割り当て。**B1/B2被りを迂回する編成選択が最適でない場合がある**（順序依存）。
- プレイヤーが最適な3属性の組み合わせを手で選べない問題を、ソルバーが解いていない。

---

## 既にある資産（活かす・ゼロからではない）
- **`_maxDisjointSquads(squads)`**（index.html:6014）— 被りなし部隊数を最大3まで判定。**プレイヤー単位の被り判定ロジックが既にUI側にある**（📌次にやることカードで使用）。
- **「🧩 3属性まとめ選び」UI**（index.html:2917）— 被りなしの3属性人気PT組み合わせを提案する画面が既にある。
- **2編成対応**（`loadoutsByAttr` slot=1|2）、ソルバーの `usedChars`（パス内被り回避は機能）、**Lv4無限ボス/温存/吸収による総与ダメ最大化**（＝目的関数は既に「合計打点最大化」）。
- → **足りないのは「完了凸の反映」と「プレイヤー単位の被りを考慮した最適3凸選択」の2点**。`_maxDisjointSquads`・🧩まとめ選びの"被りなし最適選択"部品を**ソルバー本体に統合するのが本命の筋**。

---

## 設計の方向（段階）

### フェーズ1：正しさ（最優先・低リスク・運用の本命）
- 各メンバーの `usedChars` を**完了済み凸の `characters`（記録済み5キャラ）で seed** する。編成の推測は不要、記録値を直接引く。
- → 使用済みキャラを含む編成を提案しなくなる（ラピバグ解消 + 「朝1凸→残りお任せ」フロー成立）。
- **データ依存（前提A・ユーザーOK済み）**: 完了凸に編成記録があること。無記録凸は従来どおり（被り不明）。
- リスク小。旧挙動との差分＋新テストで固定。

### フェーズ2：最適化（進化の本体）— 3案を比較（詳細は Opus5/Codex）
- **案2a：プレイヤー単位の候補列挙 + 大域割当**（現時点の推し）
  各プレイヤーの「被りなし≤3編成の組み合わせ」を列挙（≤10編成なので少数）。boss需要に対しスコアし、union全体で「誰がどのボスに何を出すか」を総与ダメ最大化で選ぶ。`_maxDisjointSquads` の考え方をソルバーに持ち込む。列挙は小規模で高速。
- **案2b：貪欲 + 局所改善（swap）**
  現行貪欲の後、「あるプレイヤーの編成を別の被りなし編成に入れ替えると union 総打点が増えるか」を反復探索。現行を活かせるが最適性は劣る。
- **案2c：割当問題として厳密最適化（ILP/最大重みマッチング）**
  最も正確だがビルドレスJSには重い。将来オプション。

### フェーズ3：「多く出す」を促すUX（データ側・並行）
- 最適化は選択肢が多いほど効く。朝1凸でB1/B2を使い切ると残り2凸に出せる編成が「そのB1/B2抜き」しかない → **主力を散らした多様な編成を多く出す**必要。フェーズ1（除外）とフェーズ3は同じコインの裏表。
- 「5属性×2編成」提出の導線強化（既存の📌カード / 🧩まとめ選び）＋「あなたの提出だと被りなし最大N部隊」の可視化（`_maxDisjointSquads` 流用）。

---

## 未解決の論点（Opus5 / Codex で詰める）
1. **計算量**: union割当を高速に保てるか（~30人 × 少数バンドル）。案2aの列挙上限・打ち切り。
2. **既存ロジックとの整合**: Lv4無限ボス/温存/吸収の目的関数に「プレイヤー被り制約」をどう載せるか（現行の貪欲を置換するのか、上に被せるのか）。
3. **完了凸の編成未記録**時の保守的な扱い（被り不明をどう安全側に倒すか）。
4. **データ不足**（1編成しか出していない人）のフォールバック（現行維持）。
5. **表示**: ソルバーが選んだ「あなたの最適3凸（属性・編成）」を分かりやすく提示（配信プラン/わたしの凸に載せる）。
6. **テスト**: 差分テスト（旧実装との等価性が要る箇所）＋新規シナリオ（部分消化・被り迂回・データ不足）を run-tests.mjs に追加。

---

## フェーズ記録（各モデルの検討を追記していく）

### Fable試行（第1案）— 2026-07-29
本文書の初版。要点: フェーズ1（完了凸キャラ除外）が運用の本命かつ最優先、フェーズ2（被り考慮の大域最適化）が進化本体、フェーズ3（多く出す導線）が燃料。`_maxDisjointSquads`・🧩まとめ選びの既存部品をソルバーに統合する筋を提示。案2a（候補列挙+大域割当）を暫定推奨。

### Opus5 再検討 — 2026-07-29

第1案を実コードで検証した結果、**フェーズ1の実装範囲が第1案の想定より広い**ことが判明。追試で確定した事実は以下。

#### 🔴 最重要: フェーズ1は「1行」では済まない — データ層の変更が必須
第1案は「完了凸の `characters` で `usedChars` を seed するだけ」としていたが、**その `characters` がソルバーに届いていない**。

- `supabaseLoadOpsDashboardData`（js/supabase-client.js:2657付近）の凸取得は
  `.select('id, player_id, attack_number, boss_number, boss_code, damage_raw, level')` で **`characters` 列を引いていない**。
- DBには記録されており、**他6箇所のクエリ（:254, :721, :1466 ほか）は `characters` を取得している** → 盤面ローダーだけが漏れている。
- `js/optimal-plan.js` は **`characters` を一切参照していない**（grep 0件）。

→ **フェーズ1の正しい実装範囲は3層**:
1. **データ層**: 盤面ローダーの SELECT に `characters` を追加（1列・低リスク）
2. **ソルバー層**: `buildMemberState` の `usedChars: new Set()`（:246）を完了凸のキャラで初期化
3. **正規化**: `characters` には旧スキーマの**画像パス混入**がある（:1488 コメント）。名前へ正規化しないと照合が効かない
   → **`_isLikelyCharName`（js/supabase-client.js:1489）が既存**（パス/拡張子/UUID/URL を除外）。**再利用できる**。

#### 🟢 追試で解消した懸念: 多パス構造との整合
ソルバーは温存(PhaseB)の probe→本パス、吸収(PhaseC)のやり直しという多パス構造だが、
**`buildMemberState()` の呼び出しは :291 の1箇所に集約**されている（コメント :180-181 の設計意図どおり）。
→ **そこで seed すれば全パスに正しく効く**。パス間の持ち越し事故は起きない。

`usedChars` の作用点も確認済み: 判定3箇所（:375 通常割当 / :537 Lv4割当 / :722 未使用診断）+ スナップショット複製（:318）。
→ **seed は全経路に自動で波及する**（判定ロジックの変更は不要）。

#### 🟡 残る要検討: 二重カウントの可能性
`usedCount`（:151-158, :193-200）は「その属性の凸回数ぶん上位ロードアウトを `slice` で除外」= **属性内の消費のみ**。
ここに `usedChars` seed が加わると、**凸済み属性側で過剰に絞られる**可能性がある（例: 鉄甲1凸済み → 鉄甲の上位編成は既に除外 + そのキャラも usedChars に入る）。
灼熱側で正しく効くか・鉄甲側で過剰にならないかは **Codex に判定を依頼**（論点A）。

#### 🔵 フェーズ2の評価を修正: 案2a は「置換」ではなく「補完」であるべき
第1案は案2a（候補列挙+大域割当）を推していたが、現行ソルバーは既に
**総与ダメ最大化 + 温存(機会費用つき2パス) + 吸収 + Lv4無限ボス + 時間軸スケジューリング**という高度な構造で、
**テスト97件と実戦検証**がある。ここを候補列挙型の大域割当で置き換えるのは**破壊的**。

- **推奨: 案2b（現行保持 + 局所改善）を主軸に、2a の発想は"候補生成"だけに使う**
- 「この編成を採ると他属性で何を失うか」という**機会費用の枠組みは既に温存 PhaseB にある**（potential 差分）。
  同じ枠組みで「キャラ被りによる機会費用」を評価できる可能性が高い
- **大域最適化が本当に必要かは、フェーズ1修正後の実データで乖離を測ってから判断**（論点C）

#### 優先度の再評価
| 項目 | 第1案 | Opus5評価 |
|---|---|---|
| フェーズ1 | 最優先・低リスク | **最優先は同意。ただし範囲は3層（データ層含む）** |
| フェーズ2 | 案2a 推奨 | **案2b 主軸に変更。2a は破壊的。効果測定を先に** |
| フェーズ3 | 燃料 | **同意。フェーズ1で「被り除外後に出せる編成ゼロ」が可視化されて初めて説得力が出る** |

### Codex 併用 — 2026-07-29

Codex(gpt-5.6-sol) の独立検討。Opus5 の追試を全面的に裏付けつつ、**4つの重要な追加論点**を提示。

#### 🔴 追加発見①: `.slice(usedCount)` の近似が seed で露呈する（二重カウントの正体）
「キャラ」と「同属性ロードアウト」は別制約なので**原理的には二重カウントではない**。ただし:

- **正しく効くケース**: 鉄甲①(ラピ入り)を凸済み / 鉄甲②はラピなし / 灼熱はラピ入り
  → `usedCount` が鉄甲①を消費、seed が灼熱を除外、鉄甲②は残る = **正しい**
- **⚠ 問題のケース**: 鉄甲**②(ラピなし・低火力)で実際に凸**したが、①(ラピ入り・高火力)が未使用
  → 現行 `.slice(usedCount)`（:193）は**ダメージ順に①を消す**（=「完了凸＝属性内の最高火力編成を使った」という**近似**）。
  そこに seed が加わると**実使用の②も消える** → **合法な①まで失う**

これは seed が作るバグではなく、**既存の近似が表面化する**もの。「正しさ」を名乗るなら放置しない。
**完了凸ごとに「どのロードアウトを消費したか」を確定させる優先順**:
1. 実行時に保存した `loadoutSlot`（**`attacks` に現在この列は無い** — supabase/01_schema.sql:65）
2. 完了凸の正規化済み5人組と**完全一致するロードアウト**
3. 解決不能な旧データのみ現行のダメージ順 `slice` にフォールバック + 診断に「**ロードアウト消費は推定**」と明示

> 今後の正確性のためには、実凸にプランの `loadoutSlot` を紐付けて保存するのが最善（将来のマイグレーション候補）。

#### 🟡 追加発見②: 実装順序の罠 — seed → avail 除外 → **その後に** mandatory 算出
`buildMemberState` 内で seed するだけでは不十分。**順序が重要**:
1. 完了凸の正規化済みキャラの和集合を `usedChars` 初期値に
2. **seed と衝突するロードアウトを `avail` から先に除外**
3. **その後**に `mandatory`（:225）と `anyTeamRegistered`（:247）を算出

でないと「**実際には出せない得意属性を予約して他属性まで止める**」（予約だけ残る）。

同様に**温存 Phase B の `potentialOf()`（:610）は残りロードアウトの上位火力を被り無視で合算**しているため、
seed 衝突候補を初期 `avail` から除けば**今回の完了凸由来の過大見積りは防げる**。
（将来的には「seed と相互被りを除いた最大k編成和」にするのが整合的）

#### 🟢 3モデルが一致した点
- フェーズ1は必須・最優先。範囲は**データ層 + ソルバー + 正規化の3層**
- `buildMemberState()`（:291）が単一入口なので**通常/温存/吸収/Lv4 に一貫して効く**。snapshot（:314）も `usedChars` を複製・復元するので安全
- **フェーズ2は現行を置換しない**（Codex「置換しないのが重要」= Opus5 の案2b主軸と一致）
- **案2a 単体は解法ではない**: 計算量自体は問題ない（1人あたり最大 C(10,1)+C(10,2)+C(10,3)=175 バンドル、30人で5,280）が、
  **ボスHP・レベル開放・時刻が全員を結合する**ため全員のバンドル直積は非現実的。`_maxDisjointSquads`（index.html:6014）も重み・ボス・時間を決めない
  → **候補生成とベンチマークに使う**
- **案2c は本番ではなくオフラインのオラクル**（最適性を測る物差し）。ブラウザ配布・タイムアウト・目的関数再現が新たな運用リスク

#### 🔵 フェーズ2の推奨形（Codex）: 2b を「限定分岐 + 全パス再実行」に拡張
現行の最終結果に対する**素朴なswapは危険**（後続Lvの開放時刻・mandatory・温存を壊す）。代わりに:
- 現行貪欲を**基準解**にし、「キャラ衝突で候補が落ちた箇所」「僅差候補」「Lv4温存候補」だけで**上位K分岐**を作る
- 各分岐は部分手直しせず、**同じ Phase A/B/C/Lv4 ロジックで最初から再実行**し、現行と同じ比較軸で採否を決める
- まず**幅4〜16・深さ1〜3**に制限すれば PWA でも説明可能な計算量

#### 📏 効果測定の具体手順（論点C・Codex提案）
過去ログだけでは当時の提出ロードアウト・時刻・強属性を復元できない → **プラン押下時の `PlanInput` スナップショットを収集**してから比較する。
1. フェーズ1後の実盤面を匿名化可能な fixture として保存、同一入力で「現行貪欲 / 限定探索 / オフライン厳密解」を走らせる
2. 比較は `totalCreditedB` 単独でなく**辞書順**: ①実行不能な提案数0 → ②有限Lv踏破数 → ③totalCreditedB → ④Lv4与ダメ → ⑤完了時刻・flex/mismatch件数 → ⑥mandatory未達数
3. **衝突圧を計測**: seed により除外された候補数 / チーム未記録で安全割当できない人数 / 貪欲解と探索解で属性・ロードアウトが変わった人数
4. 10〜30盤面でオフライン厳密解を作り差を分布で見る。「Lvが1つでも落ちる」「実行可能な総与ダメの差が継続的に大きい」「高衝突メンバーで代替案を逃す」が出た場合**にだけ** Phase 2 を本番化

#### ⚠️ Codex の警告（論点D）
- 現行は「総与ダメ最大化」を掲げつつ**有限Lvの選択自体は重み付き貪欲**。厳密最適と呼ばず、Phase 2 では目的関数・比較順を固定してから測る
- **`attacks.characters` は攻撃時のスナップショット / `player_damages.characters` は現在の登録編成** → 編成更新後は一致しなくなる。`loadoutSlot` か不変のロードアウトIDなしに完全な消費対応は不可能
- **キャラ名の表記揺れは `Set.has()` をすり抜ける** → ソルバー内でファジィ一致せず、**保存・ロード境界でcanonical化**する（通常の凸報告は既に保存前 canonical 化を試みている index.html:10215）
- 運営盤面は最大60秒古いまま算出され得る（鮮度再取得あり index.html:13173）が、**別端末で直後に凸された競合までは防げない** → 実行前の再計算・生成時刻表示は残す
- DBは「同キャラ1日1回」を制約していない。`characters: []` が残る限りソルバーが保証できるのは「**記録済み情報の範囲での安全性**」→ **プラン上で明示**しないと期待を誤らせる

---

## 【決定 2026-07-29】完了凸の characters が空の場合 = **best-effort（現行踏襲 + 警告）**

ユーザー判断: **strict（自動割当から除外）ではなく best-effort を採用**。
- 従来どおり候補に残す。ただしプランに**警告フラグ**（例 `unsafeUnknownCompletedTeam`）を立て、運営画面で**分離表示**する
- 理由: **プラン算出後に心配があれば Discord で本人に「この編成で出せるか」確認できる**（内輪運用の強み）
- → 警告は「ブロック」ではなく「**確認すべき対象を名指しする**」役割。運営の判断材料として機能させる

---

## 最終方針（3モデル合意 + ユーザー判断）

### フェーズ1（最優先・実装対象）— 完了凸キャラの除外
**実装順序（Codex の指摘を反映した確定版）**:
1. **データ層**: `supabaseLoadOpsDashboardData` の attacks SELECT に `characters` を追加（js/supabase-client.js:2659）。旧スキーマ互換が要るなら列エラー時に無しで再試行
2. **正規化の共通化**: `_isLikelyCharName`（:1490）は前シーズン引継ぎ関数内のローカル関数 → **再利用可能な共通関数に寄せる**。空白/NFKC/全角コロンの機械正規化 + パス/URL/画像拡張子/ハッシュ除外 + 可能なら `nikke_characters` の alias→canonical 解決
3. **ソルバー層**（js/optimal-plan.js `buildMemberState`）: ①seed を `usedChars` 初期値に → ②**seed 衝突ロードアウトを `avail` から除外** → ③**その後**に mandatory / anyTeamRegistered を算出
4. **ロードアウト消費の確定**: 完全一致 → フォールバック（現行 slice）+「推定」診断
5. **警告フラグ**: `characters` 未記録の完了凸を持つメンバーにフラグ、プラン表示で分離
6. **typedef とテスト更新**: `PlanInput`/`PlayerInput`（:20）+ 回帰テスト（下記）

**受け入れ条件（テスト）**:
- 鉄甲でラピ使用済み → 灼熱ラピ入りは不採用、ラピなし代替は採用
- seed 対象と同属性ロードアウトの実行順が高火力順と**逆**のケース
- seed で得意属性が全滅したとき mandatory が他属性をロックしない
- 温存/吸収/Lv4 で完了凸キャラを含む候補が復活しない
- 完了凸 characters が空 = best-effort（候補に残る + 警告フラグが立つ）
- **完了凸が無い入力は既存出力と完全一致**（回帰保証）
- 既存の衝突テスト（tests/run-tests.mjs:156）を「前状態を足す」形で自然拡張

### フェーズ2（2026-08-03 実装完了・本番化）
方式は予告どおり **2b 限定分岐 + 全パス再実行**。最終形は **幅16・深さ3**
（基準解を含め最大49シナリオ。設計時の見立て「幅4〜16・深さ1〜3」の上限に落ち着いた）。

- **2a（不要凸の除去）**: 撃破を保ったまま抜ける凸を外す後処理 `trimOverkill`。
  実機報告の盤面で損失 8.4B → 2.9B（総当たり最適と一致）
- **2b（ボス横断の限定分岐）**: 僅差の決定点で2番手を採るシナリオを解き直す。
  採用は「基準解より credited・実現可能性・踏破Lv のいずれも下げない」ことが絶対条件
- **決定点キーは2種類**:
  レベル別 `L|モード|ボス|何凸目` と全レベル一括 `*|*|ボス|何凸目`。
  貴重な人材の取り合いはレベルをまたいで連鎖するので、一括キーが無いと届かない改善がある
- **反復深化**: 改善した分岐の上にさらに分岐を重ねる（深さ3）。1決定点だけでは弱い
- **実時間で打ち切らない**: 配信（📤）は「押すたびに同じ指示が出る」ことが前提。
  上限は人数から決まる決定的な値（`scenarioBudgetFor`）

**測定**（`node tests/bench-crossboss.mjs` — seed固定の乱数2000盤面 vs `crossBoss:false`）:

| 指標 | 結果 |
|---|---|
| 改善 / 悪化 / 同一 | 767 / **0** / 1233 |
| 総与ダメの合計差 | **+16,744B**（改善盤面あたり平均 +22B） |
| 踏破レベル 上昇 / 低下 | 41 / **0** |
| 実行時間 | 中央値 58ms / p95 247ms / p99 375ms / 最大 790ms（Mac の Node 値） |

> **数字の読み方（測り直しの経緯）**: この表に落ち着くまでにベンチを2回作り直している。
> 1. 初回「改善1178 / +28,966B / 踏破Lv上昇85」→ **過大評価**。盤面が `damagesByAttr` だけで
>    キャラ被り・同属性2編成が一切評価されず、ボスHPも本番の 1/3 以下だった（Codex 指摘）
> 2. 2回目「改善577 / +7,694B」→ **今度は過小評価**。汎用キャラを共有させすぎて
>    「全員が1日1凸しかできない」退化盤面になっていた（割当率 47%）
> 3. 3回目（上の表）→ 本番 `player_damages` の実測分布に合わせた。
>    46人227編成を集計し、**編成ペアの共通キャラ数 平均0.30（0が83%/1が5%/2が12%）**、
>    1人あたり編成数 平均4.9、1人が使う総キャラ種類数 平均21.8。割当率は 97% になった。
>    生成側の実績は 平均0.29（82.4%/6.1%/11.5%）・編成数4.90 で、**ベンチが毎回この分布を
>    出力して実測値と並べて表示する**（較正を口約束にしない）
>
> **性能**: ソルバーは同期実行でメインスレッドを止める。実行時間は端末・負荷に依存し、
> 同じ盤面でも Mac の Node で最大 790ms〜1,450ms とばらつくので**上限としては読まないこと**。
> スマホではさらに3〜4倍かかる。幅を12に落とせば最悪が6割程度に下がる（品質は83%維持）が、
> 2026-08-03 にユーザー判断で**品質優先（幅16・深さ3）を採用**。
> 代わりに算出中は「🧮 最適な3凸を計算中…」を出して固まって見えないようにした。

回帰テストは実測で見つけた盤面を使い、4通りの変異（一括分岐除去・深さ1・レベルキー衝突・
横断分岐無効化）すべてで落ちることを確認済み。`crossBoss: false` で従来の貪欲解に戻せる。

### フェーズ3（並行・データ側）
- 「5属性×2編成」提出導線の強化 + 「あなたの提出だと被りなし最大N部隊」可視化（`_maxDisjointSquads` 流用）
- フェーズ1で「被り除外後に出せる編成ゼロ」が可視化されて初めて説得力が出る
