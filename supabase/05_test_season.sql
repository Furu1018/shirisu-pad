-- ============================================================================
-- Phase: テストシーズン対応
-- seasons.is_test カラムを追加
-- ============================================================================
-- テストシーズンは is_test=true で作成され、削除時に元のアクティブシーズンと
-- player_damages を復元する運用。実シーズンと混同しないためのフラグ。
--
-- 適用: Supabase Dashboard → SQL Editor で実行
-- ============================================================================

ALTER TABLE seasons
    ADD COLUMN IF NOT EXISTS is_test BOOLEAN NOT NULL DEFAULT FALSE;
