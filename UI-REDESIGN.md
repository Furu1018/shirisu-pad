# UI全面見直し — タブ構造 (IA) 議論の記録

2026-07-22 職場PCにて、Fable × Codex の2ラウンド議論で合意した案。
**ステータス (2026-07-23): ユーザー承認済み・Stage 1 実装中。**
承認前に Artifact の動くモックアップ (完成形デモ) で全タブ・主要フローを確認済み。
モックで追加合意した詳細:
- 模擬の自分ダメージ・戦況のボス一覧は **B1〜B5 の横並びカード** (現行踏襲)
- **Lv4 (Lv3踏破後) は B1〜B4 を暗転・凸不可にし B5 のみ選択可** — 判定は盤面から自動
  (ソルバーの fullyClearedThrough>=3 と同ロジックを表示側でも使う)
- 分析の属性別ダメージ分析は現行どおり **1カード内モード切替** (属性内訳⇄SLv×ダメ散布図)
- 属性名は「灼熱」(火炎ではない)
- Codex 併用方針: 同一ファイルの手術は Fable 単独 + Codex 監査 / 独立新規部品 (ユニオン事前比較等)
  は Codex 並走実装で比較採用 / 設計判断はセカンドオピニオン
- 実装は Stage 1 から: 分析統合は **ソフト統合方式** (TAB_REGISTRY に両タブを残し `navTab` で
  ナビ1ボタン化 + タブ内セグメントで相互遷移 — 描画/PTR/スワイプ機構を無傷に保つ)。
  Codex セカンドオピニオンも B 推奨で一致 (2026-07-23)。その際の判断記録:
  - **URL 契約は内部名のまま** (`?tab=ranking` / `?tab=slv-ranking` は従来どおり動く)。
    `?tab=analysis` エイリアスは使い手が居ないため見送り — Push 送信側は自前なので必要になったら足す
  - **NAV_GROUPS への正式昇格は見送り** — グループが「分析」1つの間は `navTab` + レジストリ
    コメントで足りる。2つ目のグループが生まれたら Codex 案 (NAV_GROUPS 表 + activateNavView) に昇格する
  - セグメントの遷移は必ず switchTab() (=TabController.activate) 経由 — DOM表示切替で済ませない
    (スクロール保存・TTL・active同期が崩れるため)
  - スワイプ順は **registry 定義順 = ナビの並び順**。2026-07-23 ユーザー要望で
    センターホーム型 (模擬 → 戦況 → [ホーム] → 分析 → ふるり値 → 設定) に変更 —
    下部ナビの中央にホームの浮きボタン (黒丸/アクティブ時青グラデ)。起動タブはホームのまま
  - 副産物: コールド起動 deep link (?tab=xxx) 後にナビ active が initialName で上書きされる
    既存バグを発見・修正 (Codex 指摘)

## 発注者 (ユーザー) の意図 — 議論の前提

- 第一: ユニオンメンバーがより直感的に使いやすく
- 第二: レイド当日の進行を運営チームが組みやすく
- ダメージ比較・ふるり値比較は「おまけ」だがユニオンの特色。大切に磨く
- **特に好評**: 模擬戦ダメージの事前比較 (本番の結果確定前に模擬時点で互いに比較でき、
  編成や動きを本番前に改善できる) → IA上で格上げする価値あり
- 流れ: ①構造 (このドキュメント) → ②ワイヤーフレーム議論 → ③各タブUIを ClaudeDesign で磨く

## 合意案: 5タブ維持・時系列で再配分

```
現行:  [マイページ] [ユニレ管理] [比較] [ふるり値] [設定]
提案:  [ホーム]    [模擬]      [戦況] [分析]     [設定]
        自分の状態   準備期の主役  当日の主役 事後+特色   現状維持
```

| タブ | 中身 | 変更点 |
|---|---|---|
| ホーム (旧マイページ) | 「次にやること」カード (時期依存: 準備期=未提出の模擬属性→模擬へ / 当日=次の凸→戦況へ / 事後=結果ハイライト→分析へ)。主行動1件+補助最大2件。凸報告・配信プラン・締め凸依頼応答・通知・個人設定はここに残す | 模擬提出まわりを模擬タブへ出して痩身。**凸報告は移設しない** (下記「割れた点」) |
| 模擬 (新設) | 画像OCR/手動提出 + 提出状況 + **ユニオン事前比較** (属性別の模擬ダメ/模擬ふるり値ランキング — 新機能) + 個人レーダー | 好評機能を bottom-nav 1枠に格上げ。月中ずっと生きるタブ |
| 戦況 (旧ユニレ管理) | [全員] ボス状況・オンライン/調整中・残凸メンバー / [運営ON] 現行の運営セクション全部そのまま / [シーズン外] 前回リザルト要約+提出進捗+運営準備 | 改称のみ。二層構造 (`data-ops-only`) は維持。「管理」という管理者用語をやめる |
| 分析 (比較+ふるり値統合) | セグメント「実績 / ふるり値 / シミュレータ」。既定=実績 | 両タブとも確定シーズンデータ読みの同文脈なので統合。ふるり値は第二階層で特色維持 |
| 設定 | 現状維持 | — |

- レガシー characters タブは TAB_REGISTRY からも廃止 (実体は分析>実績>編成分析に既にある)

## 議論で割れて修正された点 (重要)

1. **凸報告を戦況へ移すか**: Codex 初案は「見る⇄凸る⇄報告を戦況で完結」。
   反論「レイドは月数日で、移設すると戦況が月の大半死にタブになる。Push遷移先URL
   (`?tab=mypage&focus=finishreq`) 等の依存も壊れる」→ **ホームに残し相互リンクで連携**に修正合意。
2. **ホーム痩身しすぎ**: 「次にやること」だけでは開く動機が薄い → 主行動1件+補助2件までの
   カード構成 (次にやること + 個人準備カード + 当日カード + 通知) に具体化。

## 段階移行 (各Stage単独デプロイ可)

1. **Stage 1 (低リスク)**: 分析統合 (比較+ふるり値) + ops→戦況改称 + characters レジストリ廃止
2. **Stage 2**: 模擬タブ新設 + マイページ→ホーム痩身。
   ユニオン事前比較は `renderOpsRemainingList` の集計ロジック (属性別・ダメ/ふるり切替) を転用。
   UX要件: 未提出者はランキング外で人数表示 / 同属性2編成の採用値 (最高値か編成①か) を明示 /
   基準者固定・SLv補正の説明を固定表示 / 当日残凸一覧の概念 (オンライン/残凸フィルタ) を混ぜない
3. **Stage 3**: ホーム⇄戦況の当日導線の相互リンク・仕上げ (移設はしない)
   → 実装済み (2026-07-23): 戦況タブに「凸報告はホームから→」リンク (data-ops-off-only)。
   ホーム→戦況は 📌次にやることカード (当日判定時) が担う

移行時の注意 (Codex 指摘): タブのロジック面 (swipe/PTR/CSS/描画分岐) は TAB_REGISTRY/
TabController に集約済みだが、**タブの追加・改称には registry 1行に加えて「自タブのHTML +
ナビHTML 3箇所 (bottom-nav/ヘッダ/drawer)」の手修正が必要** (ARCHITECTURE-AUDIT.md §4
ステップ5「完了条件との差分」参照)。さらに**画面外導線 (ヘルプ15章・URL パラメータ・
Push 遷移先) も自動追随しない** — 各Stageで棚卸し必須。

## 不採用の代替案 (再燃したとき用)

- **A: 4タブ最小変更** ([マイページ][戦況][分析][設定]、模擬はマイページ内に残す)
  → 学習コスト最小だが、特色の模擬比較を格上げできないので不採用
- **B: 6タブ運営分離** ([ホーム][模擬][戦況][運営][分析][設定])
  → スマホ bottom-nav に6枠は窮屈 + メンバーに不要な「運営」概念が常設されるので不採用。
  現行 `_opsMode`/`data-ops-only` で同一タブ内二層化できており分離の必然性が低い

## 次のアクション (自宅PC)

1. ユーザーと構造案の最終確認 (特に「凸報告はホーム残留」で良いか)
2. 承認後、ワイヤーフレーム議論 (各タブのセクション順・カード構成) — Codex 併用
3. その後 Stage 1 から実装。各タブのUIは ClaudeDesign で磨く

Codex セッション (続きから議論する場合): `codex resume 019f88b9-77e3-7131-b6e9-816f29b9ceaa`
