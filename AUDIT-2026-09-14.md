# システム全体監査 2026-09-14

> **進み具合 (2026-09-14)**: 🔴 #1〜#4 と 🟠 #5 は修正済み (`ea3e036` / `7b60cdb`)。**SQL 47 の適用が要る**。
> 🟡 #6〜#10 と 🔵 #11 #12 は未着手 (ROADMAP「🩺」に順番あり)。修正後の Codex 監査: A〜G 問題なし。高2・中2・低3 → c902a01 で高2・中2・低1 を修正 (低5「同じボスに requested があると予約なしで accepted」は従来からの挙動・据え置き)。#6 は誤指摘として取り下げ。その再監査 (c902a01): A〜D 問題なし・低2 = 名乗り直し中の 📌 承認 (→ f886250 で修正済み) / 世代ガードのテストが形だけ (誤指摘: 実行テストは配線テスト側にあり変異で確認済み)

対象: `main` = `e004e89` (2026-09-13)。40k行 / ドメイン25本 / SQL 46本 / テスト 818件。
やり方: Codex (gpt-5.6-terra・read-only) に **3観点で並行**に読ませ、**指摘はすべて Claude が自分でコードを開いて裏取り**した。
裏取りで否定できたものは落としてある (1件あり・末尾)。

- 観点1 「1人3凸」という希少資源の取り合い (予約 / 📌固定 / 締め凸 / 配信プラン)
- 観点2 役割 (master/ops)・名乗り・通知・並行更新
- 観点3 全体の健全性 (重複ロジック / 握りつぶし / 描画の実行時安全性 / 性能 / テストの質)

---

## 🔴 致命的 (4件) — 「3凸」が破れる / 「無い」と嘘をつく

### 1. 📌固定 と 本人の申請 が、お互いを数えていない

同じ人に **📌3件 + 申請3件 = 6件**が成立する。

| 数える側 | 数える集合 | 入っていないもの |
|---|---|---|
| `canPin` ([reservations.js:412](js/domain/reservations.js)) | `isPromise \|\| isPin` | **requested** |
| `capacityLeft` ([reservations.js:402](js/domain/reservations.js)) | `ACTIVE` = requested/approved/cancel_requested | **pinned** |
| SQL 39 容量トリガ | requested/approved/cancel_requested | **pinned** |
| SQL 45 📌トリガ | approved/pinned + 承認済み cancel_requested | **requested** |

一意索引も別々 (42 は active、45 は pinned)。
→ 運営が3件承認した瞬間、本人は**引き受けられない 📌 を3つ抱える**。

直し方: 「実凸 + 約束 + 📌 + 申請」を**一つの容量式**に統一する。少なくとも `canRequest` と SQL 側で 📌 との相互排他を持たせる。

### 2. 締め凸の「了承」が 📌 を無視して4件目の固定を作る

[`_reserveForFinishRequest`](index.html) は `status: 'approved'` を**直接 INSERT** する。
重複チェックは `isActive(r) && 同じボス` だけなので、**同じボスの 📌 すら見ていない**。
DB側も素通り — SQL 39 は `pinned` を数えず、SQL 45 は `IF NEW.status <> 'pinned' THEN RETURN NEW;` で即座に抜ける。

→ 📌3件の人に締め凸を了承させると**固定が4件**。ソルバーは `isFixed = isPromise || isPin` で読むので、**3枠の人に4凸ぶん配る**。

さらに、予約の INSERT が失敗しても `finish_requests.status='accepted'` は残る。
この accepted は**予約の指紋にも `planDiff` にも現れない** → 配信プランはその了承を知らないまま3凸を配る。

直し方: 返答と予約化を**単一 RPC** にし、📌・申請・実凸を含む共通の容量検査をトランザクション内で行う。予約化できないなら accepted を確定させない。

### 3. 締め凸依頼の取得失敗を「依頼なし」として表示する

[`supabaseLoadFinishRequests`](js/supabase-client.js) の末尾が `catch { return []; }`。
コメントは「テーブル未適用環境では空扱い」だが、**catch は無条件**なので通信エラーでも `[]` を返す。
10秒ポーリングが `_finishReqCache` を `[]` で上書きし、**未回答の締め凸依頼が本人のバナーからも運営の「直近の動き」からも消える**。

直し方: 通信・DBエラーは `null` か例外にし、`[]` は本当に0件のときだけ。呼び出し側は失敗時に前のキャッシュを保ち「取得失敗」と出す。

### 4. メンバー状況が取得失敗時に「全員未登録」を捏造する

[`supabaseLoadMemberStatusExtras`](js/supabase-client.js) の `safe()` が、**あらゆる失敗を `[]` に変換**する。

- Push取得が失敗 → 実際は登録済みでも**全員「Push未登録」**
- SLv取得が失敗 → **全員「今季SLv未登録」**
- 締め凸取得が失敗 → **「未返答 0」**

★ 同じ関数の中の `availConfirmations` だけは「`null` = 未適用」を正しく区別している。
**設計は分かっているのに、他に適用されていない**。

直し方: extras を「配列 / `null` (未取得)」で区別し、`memberStatusDomain` に未取得を渡す。`availConfirmations` の契約に揃える。

---

## 🟠 高 (1件)

### 5. 締め凸の返答に CAS (条件付き更新) が無い

`supabaseRespondFinishRequest` は現在の status を確認せず UPDATE する。
同じ依頼を2タブで開き、タブ1で「了承」(→ approved 予約ができる) → 遅れてタブ2で「難しい」を押すと
`accepted → declined` が通り、**作られた予約は解除されない**。画面は辞退なのにプラン側は固定のまま。

直し方: `status='pending'` を条件に付ける。予約に `source_finish_request_id` を持たせ、辞退・取消で対応予約を処理できるようにする。

---

## 🟡 中 (5件)

### 6. 催促Push が時間帯フィルタを迂回する
`_opsNudgeGroup` が `ignoreAvailability: true` を**常に**渡す ([index.html](index.html))。
Edge Function はこのフラグで時間帯フィルタを完全に飛ばす ([send-push/index.ts:88](supabase/functions/send-push/index.ts))。
緊急通知だけの迂回のはずが、前日の「模擬未提出 / 時間帯未確認」催促も通る。
→ 催促は `ignoreAvailability` を渡さない。締め凸・予約失効など緊急な経路だけ `true`。

### 7. 期限切れの 📌 が自動解放されない
`ask_deadline_at` を過ぎても残り、ソルバーは固定として扱い続ける。
📌 は普通 `raid_level=null` なので、撃破・レベル進行時の `_releaseInfeasibleReservations` も拾わない。
→ 期限切れ📌を「運営の確認待ち」として目立たせ、一括解除の導線を置く。

### 8. 📌 の置き直し・お願いに楽観ロックが無い
運営A・Bが同じ📌を開き、Bが先に動かしても、Aの古い画面からの移動が `id` と `status='pinned'` だけで通る。
`AskPin` も `asked_at IS NULL` を条件にしていないので、二人が同時にお願い・Push できる。
→ `updated_at` を一致条件に含める。`asked_at IS NULL` も条件に。

### 9. SLvシミュレーターが `NaN%` を出す / ドメインを迂回している
実績ダメージ0の人を選んで目標SLvを入れると `0 / 0` を `toFixed()` して `+0 (NaN%)`。
ドメインの `predictDamage` は0を `null` で拒否するのに、画面が独自に比例計算している。
→ `runSimulator()` も `slvSimDomain.predictDamage()` を通し、`null` なら入力不足を出す。

### 10. 名乗り直しの後着ガードが一部に無い
`_syncOpsRole` と `renderMyReservations` は**現在のIDしか照合していない** (世代カウンタが無い)。
A→B→A と戻すと、最初のA向け応答が「IDが一致する」ので通り、新しい結果を上書きする。
アバターとメンバー状況は世代で守られているので、**設計は分かっているのに一部に無い**状態。
→ `setCurrentIdentity` ごとに世代を進め、開始時に捕まえて完了時に ID と世代の両方を照合する。

---

## 🔵 低 (2件)

### 11. 10秒ポーリングが全タブで常駐する
`_startCoordPolling` は `document.hidden` では止まるが、**アプリ内でタブを移っても止まらない**。
10秒ごとに 締め凸依頼 + 調整中一覧、3回に1回 役割 + ボスHP。31端末ぶんが常時流れる。
→ ホーム / 運営タブがアクティブなときだけ回す。

### 12. 到達しない旧トグル関数が残っている
`toggleOpsPlanSticky` / 旧 start-mode ラッパーは UI から呼ばれていないのに、
テストが「宣言があること」だけを見て守っている。
→ 削除する (外部互換でないなら)。

---

## ✅ 問題なしと確認できたもの

| 観点 | 結果 |
|---|---|
| 予約の遷移表 | SQL 45 と JS の `TRANSITIONS` が一致。**テストが SQL から抽出して突き合わせている** (形だけでない良いガード) |
| 予約の状態集合 | SQL の CHECK と JS の `STATUS` が完全一致 |
| 直列化 | `pg_advisory_xact_lock` の鍵が 39 / 40 / 45 で同一 |
| 予約の楽観ロック | `p_expect_from` が全遷移で必須 (NULL は RPC が弾く) |
| 通知の宛先 | 6種の運営宛イベントがすべて `_notifyOps` を通り、master + ops (書庫除外・本人除外) |
| master の保護 | クライアントは `master` を書けず、既存 master も更新条件から外れている |
| ソルバーの性能 | 探索予算60・枝16・深さ3で上限あり。📌 が増えても探索木は増えない (最大2回解くだけ) |
| 重複ロジック | 属性相性は `weaknessPtOf()`、締め凸置換は `totalWithKillSubs()` に寄っている。数値差を生む別実装は SLvシミュレーター (#9) だけ |
| 2列レイアウト | 準備 `[7,5]` / 前日 `[12][7,5][12]` / 当日 `[12][7,5][7,5][12][12]` / 終了 `[5,7][12]` / メンバー `[7,5][12]` — **全段階で各行12**。段階4の📈📈も組に入っている |
| ドメイン25本 | 全部 index.html から読まれ、全部テストに登場 |

---

## テストの質

**良い例 (実際に関数を実行している)**
- `tests/ops-pace.mjs` — `renderOpsPace` / `renderOpsRecent` を抽出実行。24時間・停滞・全消化・時刻欠損・`NaN` 非混入まで
- `tests/member-board.mjs` — `_mbPaint` を実行し、**非同期応答の逆転**まで見ている
- `tests/mock-panels.mjs` — `renderMyDamagePanels` を実行 (2026-09-07 の本番障害を受けて追加)

**弱い例 (ソースの文字列を見ているだけ)**
- `run-tests.mjs:6061` — `toggleOpsPlanSticky` の**宣言があること**だけ (#12 の死んだ関数を守ってしまっている)
- `run-tests.mjs:8360` — `simKillBox` の HTML 断片があることだけ
- `run-tests.mjs:8377` — `data-name` 属性の文字列一致だけ

**テストが無い重要な領域**
- `runSimulator()` の0ダメージ・欠損DOM・締め凸置換
- `renderGbCompare()` 全体 (いまは `_gbMembersHtml` だけ)
- `renderOpsPlanView()` / `_planTimetableHtml()` の DOM 実行 (いまは正規表現)
- 👑 任命パネルの実描画・通信失敗時の表示
- **「失敗は空ではない」契約テスト** (#3 #4 を捕まえるガード)
- A→B→A の後着応答 (#10)
- `send-push` の時間帯フィルタ / `ignoreAvailability` (#6)

---

## 裏取りで否定した指摘 (1件)

Codex は「任命パネルが 運営タブ › その他 と 設定タブ の2か所にあり、片方が古い」と報告したが、**誤り**。
実際は設定タブのみで、CLAUDE.md:542 も正しく「設定タブ」と書いている。
原因は**私が渡した前提が誤っていた**こと (コミット `e9ed6c0` の古い記述を信じた)。
→ 教訓: 監査の依頼文に書く「前提」もコードで確かめてから渡す。

---

## 直す順番の提案

1. **#3 #4** (握りつぶし) — いちばん安く、いちばん怖い。「無い」と嘘をつくのを止める
2. **#2 #1** (枠の二重確保) — 当日の運営が壊れる。#2 が先 (単独で4凸が配られる)
3. **#5 #8** (CAS・楽観ロック) — 二人運営なら必ず踏む
4. **#6** (催促Push) — 1行。夜中に通知が飛ぶ
5. **#9 #10 #7 #11 #12**

★ どれも**先にテストを書いてから**直すこと。#3 #4 #10 は「失敗は空ではない」「後着は捨てる」の契約テストが無いために生き残った。
