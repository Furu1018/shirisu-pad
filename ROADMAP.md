# しりすこPAD ロードマップ / 引き継ぎメモ

最終更新: 2026-07-08 (Claude Fable 5 セッションの総括)。
新しいモデル・PCで開発を始めるときは **CLAUDE.md → .claude/skills/ の4スキル → このファイル** の順に読むこと。

## 現状サマリー

「前日にデータを集める → 当日回す → 月次で分析する」の3フェーズが一通り完成した状態。

- **前日**: 模擬提出 (1属性2編成対応・OCR)・SLv自己申告・得意属性・戦闘可能時間 (⏳隙間型/ハイブリッド)。
  運営は 🪞模擬提出チェック→催促Push。特典設計 = 5属性提出でレーダー完成 / ふるり値トップ
- **当日**: プラン算出 (得意属性必須消化・同属性2凸・時間帯割当・律速・60秒鮮度保証)
  → 📤配信 → 凸報告 (OCR/重複ガード) → 🔍提出漏れチェック → 再算出→再配信。
  HP鮮度⚠️ + 30秒ポーリング。運営モードでメンバー画面はすっきり
- **月次**: ふるり値タブ (確定値)・ダメージ比較・シミュレータ (Haiku Vision OCR)
- 検証: 単体テスト32件 (`node tests/run-tests.mjs`) + 構文/参照/CSS値の機械チェック

## 次のレイドで実戦検証すること (最優先タスク)

- [ ] 前日: 模擬提出チェック→催促Push の流れ / 編成②の提出とカード入れ替えの操作感
- [ ] 当日: 算出→配信→凸報告→再算出 のループが実際に回るか
- [ ] 提出漏れチェックのOCR精度 (名前不一致が多ければ `_OPS_AUDIT_PROMPT` を実スクショで調整)
- [ ] プランに律速⏱・⏳隙間・同属性2凸(②)が正しく現れるか
- [ ] 初回読み込みと再訪問の体感速度 (sw.js 画像キャッシュの効果)

→ 引っかかった点はこのファイルに追記してから直すこと (症状→原因→修正の記録が次に効く)

## 既知の小さな穴 (実害軽微・優先度低)

- `attacks.attack_number` に一意制約がなく、同時保存で番号重複しうる (表示上のみの影響)
- 通知の既読は端末ローカル (localStorage)。機種変更で未読に戻る
- 得意属性が時間帯の都合で消化できない日、その人の自由枠が塞がれたままになる (稀なケース)
- `renderMyDamageInputs` (旧リスト型の模擬入力UI) は呼び出し元なしの休眠コード。消してよい
- 提出漏れチェックは「0凸の人は全体スクショに写らない」前提 (📷別枠表示で運用カバー済み)

## 機能候補 (優先度順)

1. **レイド実戦のフィードバック対応** — 最優先。内容は使ってみるまで未定
2. Supabase Realtime 化 — ボスHP/凸のポーリング(30秒)を購読に置き換え、当日のライブ感を上げる
3. Material 残骸の細部統一 — 色はトークン再マップ済み。旧モーダルの角丸/余白などの形状のみ残り
4. 通知既読のDB化 — 端末間で既読同期 (テーブル追加が必要)
5. `attack_number` のユニーク制約 + 衝突時リトライ
6. レーダーの「ボス順表示」オプション — 現状は月をまたぐ形の比較可能性を優先して固定順

## 運用ルール (ヘルプ13章「当日運用チェックリスト」と同期)

- **レイド当日は main へ push しない** (push=即本番デプロイ)。仕込みは前日まで
- 開始前に 📸プラン画像を Discord へ (インフラ障害時の紙のバックアップ)
- 当日の「シーズン作成」ボタンは触らない (模擬全クリア。件数つき警告あり)
- Discord告知はユニレ管理タブ下部の「📣 Discord告知テンプレ」からコピー

## 前提SQL (2026-07-08 時点ですべて適用済み)

新規環境を作る場合は `supabase/` を番号順に実行。既存環境は 12 / 17 / 18 / 19 / 20 / 21 が
機能の前提 (詳細は CLAUDE.md)。Edge Function (`analyze-image`=dynamic-service, `send-push`) は
変更時に再デプロイが必要。カスタムプロンプトはクライアントから注入できるので、
OCRの新タスク追加だけなら再デプロイ不要 (`callAiVision(url, 'task名', { prompt })`)。
